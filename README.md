# Elemental Aerial Arts — dreamey.io

Rebuild of santabarbaraaerial.com. Vite + React + TypeScript, deployed on Cloudflare Pages.

- `npm run dev` — local dev
- `npm run build` — production build to `dist/`

Design tokens in `src/index.css` (colors/fonts pulled from the original site).
